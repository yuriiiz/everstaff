repos:
  - repo: local
    hooks:
      - id: check-log-format
        name: Check log format conventions
        entry: bash scripts/check-log-format.sh
        language: system
        types: [python]
        pass_filenames: true
